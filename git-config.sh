#!/bin/bash
git config --global user.name "Eric Lewis"
git config --global user.email eric.andrew.lewis@gmail.com
# Use VSCode as the default editor for git
git config --global core.editor "code --wait"
# Set `main` as the default branch name
git config --global init.defaultBranch main